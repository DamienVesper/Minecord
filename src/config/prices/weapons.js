const prices = {
    swords: {
        stone: [1e3, `cash`],
        iron: [75e2, `cash`],
        gold: [75e3, `cash`],
        diamond: [5e5, `cash`],
        netherite: [1e7, `cash`]

        // inferno: [1e8, `cash`],

        // christmas: [1e3, `candy`],
        // dragon: [20, `lapis`],
        // corona: [1e3, `germs`]        
    },
    pickaxes: {
        stone: [1e3, `cash`],
        iron: [75e2, `cash`],
        gold: [75e3, `cash`],
        diamond: [5e5, `cash`],
        netherite: [1e7, `cash`],
        mystery: [12e3, `emerald`],
        rainbow: [5e3, `emerald`]

        // dirt: [1e3, `lapis`],
        // emerald: [25e2, `emerald`],

        // inferno: [1e8, `cash`],

    },
    axes: {
        stone: [1e3, `cash`],
        iron: [75e2, `cash`],
        gold: [75e3, `cash`],
        diamond: [5e5, `cash`],
        netherite: [1e7, `cash`],
        fire: [300, `emerald`],
        battle: [500, `emerald`]

        // inferno: [1e8, `cash`],

    }
}

module.exports = { prices }